const Navbar = () => {
  return (
    <header
      style={{
        background: "#fff",
        padding: "20px 30px",
        borderBottom: "1px solid #ddd",
      }}
    >
      <h2>QA Analytics Dashboard</h2>
    </header>
  );
};

export default Navbar;
